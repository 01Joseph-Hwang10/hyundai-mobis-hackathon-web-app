/**
 * @description
 * This is pascal case as it serves special role.
 */
export const NotImplemeneted = () => {
  alert('Not implemented');
};

export const wait = (ms: number) => new Promise((resolve) => setTimeout(resolve, ms));
